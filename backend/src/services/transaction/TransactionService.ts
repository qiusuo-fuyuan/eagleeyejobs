import { Transaction } from "../../models/Transaction";


export class TransactionService {
    getTranactionHistories(internalUserId: string): Promise<Transaction> {

    }

    createNewTransaction(internalUserId, ): Promise<Transaction>
}

/**
 * Create a global db connection. Then in services, we can use 
 * import db from 'db'. We use mongoose. The idea is that we initialize the 
 * db only once. (Keep in mind, design is always simple)
 * 
 * https://mongoosejs.com/docs/models.html
 * 
 * db.save(job)
 */

class DB {

}
